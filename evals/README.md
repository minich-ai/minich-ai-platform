# Evals

This directory is reserved for future automated evaluation tooling (scripts, LLM-as-judge harnesses, CI checks).

Authoritative tutoring behavior tests for each skill live alongside the skill:

```
skills/<skill-id>/TESTS.md
```
